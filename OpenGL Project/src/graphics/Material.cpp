#include "Material.h"

/*
template<typename diffType, typename specType, typename roughType>
Material<diffType, specType, roughType>::Material(diffType albedo, specType specular, roughType roughness)
{
	m_albedoColour = albedo;
	m_specColour = specular;
	m_roughness = roughness;
	if (!init())
		std::cout << "One or more of your variable types are invalid" << std::endl;
}

struct Texture
{
	int id;
	enum TYPE
	{
	DIFFUSE, NORMAL, SPECULAR,
	};
}



template<typename diffType, typename specType, typename roughType>
bool Material<diffType, specType, roughType>::init()
{
	switch (typeid(diffType))
	{
	case typeid(vec4) :
		
		break;
	case typeid(vec3) :
		break;
	default:
		return false;
	}
	switch (typeid(specType))
	{
	case typeid(vec4) :
		break;
	case typeid(vec3) :
		break;
	default:
		return false;
	}
	switch (typeid(roughType))
	{
	case typeid(vec4) :
		break;
	case typeid(vec3) :
		break;
	default:
		return false;
	}
}
*/



